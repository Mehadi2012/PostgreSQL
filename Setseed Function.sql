SELECT SETSEED(0.5);

SELECT RANDOM();   -- 0.2499

SELECT RANDOM();   -- 0.5200

SELECT SETSEED(0.5);

SELECT RANDOM();  -- 0.2499   here we got the same value;

SELECT RANDOM();   -- 0.5200 here we got the same value;
